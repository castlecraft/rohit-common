Migrated from V12 to V16

Skipped Not Required Patches Check for this before_migrate_patches (runs before_migrate)

https://docs.google.com/spreadsheets/d/1TUkAwRF6NhEAzFjcM6WAPyT6dEdREkuDl5sANb-eK4o/edit?pli=1&gid=1625746251#gid=1625746251

Fixed Issues:
#1 Sales Invoice (Unable to save SI & E-Invoice)(Status: Half Resolved)
-> Removed custom code where place of supply is reset that was causing Issues
   check validations/sales_invoice.py ( method: check_local_natl_tax_rules)

   Have Enabled E-Invoice with India Compliance Need to setup again in case of new site setup
   go to : gst settings and Enable (currently in our setup sandbox mode is enable)
         for setup : https://docs.indiacompliance.app/docs/ewaybill-and-einvoice/gst_settings

   Pending :
        - removed whole validation as we are using india compliance
        - e-invoice needs to add QR Code and Ack No + Ack Date 

#2 File List (Unable to Search for File Name from Attached to Doctype)
-> corrected hook path and method check override_whitelisted_methods (rohit_common.core.file.get_files_by_search_text) hook

#3 File List (Mainly System Manager Unable to see file not attached to any doctype)
-> Overrided frappe File methods as frappe is reversing has_permission hook
   https://github.com/frappe/frappe/blob/develop/frappe/permissions.py#L467
   Check overrides/file.py and hook level __initi__.py

#4 GST Return Status (Pending)(Unable to get the GST Return Status): 
-> Need to check Will be replaced with india_compliance api

#5 Address (Pending)(Unable to Get the GST Validation from GST API)
-> Need to check Will be replaced with india_compliance api

#6 GL Report (General Ledger reports should show only nett opening)
-> Resolved Created Custom Report: RIGPL General Ledger

#7 Asset (Asset Customizations to be corrected)
-> Pushed a fix currently Under Review

#8 Asset Report (Report on Asset Analysis to be corrected)
-> Pushed a fix currently Under Review

#9 Contact (Search contacts based on search fields in the Customize form)
-> Need to check

#10 Pricing Rule (Sales Orders not fetching Pricing Rule for customers based on Item Group child like for HSS Tools and its children in v12 it used to work)
--Skipped Not related to this app

#11 Asset (Patch Failing):
-> Fixed Patch Fixed

#12 Sales Invoice (SI einvoice needs to send only summary of items to GSTIN on json instead of individual items with rates)
-> Need to check

#13 & #14 AR Report
-> Payment Ledger Was also empty 
  patch was skipped and it was introduced in later version and all doc were submitted
  so have added a script to re-populate payments ledeger check custom_method.py (backfill_payment_ledger method)